{% macro render(type, attributes = []) %}
    <html-component
            type="{{ type }}"
            {% for attr in attributes %}
                {{ attr }}
            {% endfor %}
    >
    </html-component>

    {% block javascripts %}
        {{ encore_entry_script_tags('html_elements') }}
    {% endblock %}
    {% block stylesheets %}
        {{ encore_entry_link_tags('html_elements') }}
    {% endblock %}
{% endmacro %}