<script>
	window.API_DATA_PROPS = {
        TOKEN: "{{ not app.user ? 'null' : 'TODO' }}",
        REFRESH_TOKEN: 'todo',
        BASE_PATH: "{{ app.request.getSchemeAndHttpHost() }}"
    }
</script>
